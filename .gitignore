.venv/
__pycache__/
.mypy_cache/
.ruff_cache/
submission.zip
*.pgn
.venv-dev/
snapshots/
data/

# Tuned evaluation weights, until an arena says they are better than the hand-set numbers.
# harness/package.py ships weights/ automatically, so an unproven file left here would go out
# with the next zip. Force-add it (git add -f) at the point it is proven, not before.
weights/
